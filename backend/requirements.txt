# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
websockets==13.1
python-multipart==0.0.12

# HTTP Client
httpx==0.27.2

# Audio Processing (STT only)
SpeechRecognition==3.10.4

# AI/ML
google-genai>=1.0.0

# Data Validation - Use version with pre-built wheels
pydantic>=2.0.0,<3.0.0

# Note: TTS (gTTS, pydub) removed - text-only mode
# Note: ffmpeg not required - no audio conversion needed
